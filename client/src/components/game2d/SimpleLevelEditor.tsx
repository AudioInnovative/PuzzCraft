import MobileEditor from './MobileEditor';

// Simply use our new optimized mobile editor component instead of the original code
export default function SimpleLevelEditor() {
  // Use the new mobile-optimized editor for all devices
  return <MobileEditor />;
}